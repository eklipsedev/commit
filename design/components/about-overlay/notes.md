Person about overlay (shared overlay shell with offerings).

Fields (on \`person\` document, Overlay group):
- name
- role
- bio (portable text)
- backgroundColor
- textColor

Card presentation (Card group): photo, cardBackgroundColor, cardHoverBackgroundColor, buttonBackgroundColor, buttonTextColor.
