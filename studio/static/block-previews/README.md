# Section preview images (legacy)

Insert-menu thumbnails are now managed in Sanity Studio:

**Content → Section previews**

Upload a screenshot per page-builder section there. Those images power the
Insert menu grid view automatically.

This static folder is no longer required.
